# ARP-Visualization
Built a 3-PC network in Cisco Packet Tracer to study ARP behavior at the packet level. Confirmed an empty ARP table, sent a ping, and watched the device resolve an IP to a MAC address in real time using Simulation mode. Reinforced how ARP's implicit trust model is the same mechanism exploited in spoofing attacks.
